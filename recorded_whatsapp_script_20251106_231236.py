#!/usr/bin/env python3
"""
Auto-generated WhatsApp automation script
Generated on: 2025-11-06 23:12:36
Total actions recorded: 26
"""

from pywinauto.application import Application
from pywinauto import Desktop
import time

def run_recorded_actions():
    """Execute the recorded WhatsApp actions."""
    # Connect to WhatsApp
    try:
        app = Application(backend="uia").connect(title="WhatsApp")
        print("✓ Connected to WhatsApp Desktop")
    except Exception as e:
        print(f"✗ Could not connect to WhatsApp: {e}")
        return

    main_win = app.WhatsApp
    desktop = Desktop(backend='uia')

    # Execute recorded actions

    # Action 1: Key press 'up' recorded at 2025-11-06T23:12:40.830314
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 2: Key press 'up' recorded at 2025-11-06T23:12:41.008822
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 3: Key press 'up' recorded at 2025-11-06T23:12:44.389193
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 4: Key press 'up' recorded at 2025-11-06T23:12:44.498801
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 5: Key press 'up' recorded at 2025-11-06T23:12:44.835099
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 6: Key press 'up' recorded at 2025-11-06T23:12:44.976178
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 7: Key press 'up' recorded at 2025-11-06T23:12:45.279642
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 8: Key press 'up' recorded at 2025-11-06T23:12:45.417225
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 9: Key press 'up' recorded at 2025-11-06T23:12:45.694228
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 10: Key press 'up' recorded at 2025-11-06T23:12:45.812209
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 11: Key press 'up' recorded at 2025-11-06T23:12:46.027035
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 12: Key press 'up' recorded at 2025-11-06T23:12:46.176312
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 13: Key press 'up' recorded at 2025-11-06T23:12:46.391597
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 14: Key press 'up' recorded at 2025-11-06T23:12:46.512020
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 15: Key press 'up' recorded at 2025-11-06T23:12:46.698082
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 16: Key press 'up' recorded at 2025-11-06T23:12:46.846286
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 17: Key press 'up' recorded at 2025-11-06T23:12:47.089907
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 18: Key press 'up' recorded at 2025-11-06T23:12:47.220644
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 19: Key press 'up' recorded at 2025-11-06T23:12:47.490203
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 20: Key press 'up' recorded at 2025-11-06T23:12:47.604026
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 21: Key press 'up' recorded at 2025-11-06T23:12:47.978463
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 22: Key press 'up' recorded at 2025-11-06T23:12:48.122887
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 23: Key press 'up' recorded at 2025-11-06T23:12:48.491719
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 24: Key press 'up' recorded at 2025-11-06T23:12:48.637979
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 25: Key press 'up' recorded at 2025-11-06T23:12:48.921573
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    # Action 26: Key press 'up' recorded at 2025-11-06T23:12:49.032438
    try:
        # Send key press to the active window
        main_win.type_keys('{UP}')
        print('✓ Pressed key: up')
    except Exception as e:
        print(f'✗ Could not press key up: {e}')
    time.sleep(0.5)  # Wait between key presses

    print("✓ All recorded actions completed")

if __name__ == '__main__':
    run_recorded_actions()